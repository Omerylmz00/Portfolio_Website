import 'package:flutter/material.dart';

class AppColors {
  // Çekirdek palet
  static const night = Color(0xFF0D0D0D); // ana zemin
  static const charcoal = Color(0xFF42515E); // koyu yüzey
  static const ecru = Color(0xFFC9B885); // altın vurgu (CTA/brand)

  // Tamamlayıcı tonlar
  static const champagne = Color(0xFFE0D3AC); // ecru açık tonu (hover)
  static const brass = Color(0xFFA18F6A); // ecru koyu tonu
  static const gunmetal = Color(0xFF1B232B); // kart/section zemini
  static const steel = Color(0xFF2F3A44); // border/divider
  static const slateBlue = Color(0xFF7A8EA3); // ikincil vurgu (link/ikon)

  // Metin
  static const textLight = Color(0xFFF2F2F2);
  static const textMuted = Color(0xFFC9CED6);
}
